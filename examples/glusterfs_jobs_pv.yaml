apiVersion: v1
kind: PersistentVolume
metadata:
  name: gfs-jobs-node
spec:
  capacity:
    storage: 48Gi
  accessModes:
  - ReadOnlyMany
  glusterfs:
    endpoints: glusterfs-cluster
    path: '/jobs'
    readOnly: false
  persistentVolumeReclaimPolicy: Retain
